// Navigation functionality - Brands dropdown removed
document.addEventListener('DOMContentLoaded', () => {
    // Update shop logo if configured
    if (typeof updateShopLogo === 'function') {
        updateShopLogo();
    }
});
