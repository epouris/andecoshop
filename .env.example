# Database
DATABASE_URL=postgresql://user:password@localhost:5432/andecoshop

# Server
PORT=3000
NODE_ENV=development

# Admin Credentials (change these!)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme123

# Optional: public site URL for server-side PDF brochures (defaults to http://127.0.0.1:PORT)
# PUBLIC_APP_URL=https://your-domain.example

# Optional: custom Chromium for PDF (Linux/Railway); leave unset to use Puppeteer's bundled browser
# PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
